import 'package:flutter/material.dart';
class Grid_Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Grid Screen',style:TextStyle(fontSize: 20.0)),
      ),
    );
  }
}
